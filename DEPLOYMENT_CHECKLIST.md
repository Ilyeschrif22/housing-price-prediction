# Deployment Checklist

- `requirements.txt` installs successfully
- `python train_model.py` completes and creates `models/`
- `python app.py` starts locally
- `python test_api.py` returns `200` for `/health` and a JSON prediction for `/predict`

